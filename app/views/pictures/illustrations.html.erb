<div class="container">
	<div class="row">
		<h1 class="signin-title">イラスト</h1>
		<% unless params[:user_id] %>
			<%= render "genres/genre", genres: @genres %>
			<div class="col-md-9 picture-image">
				<%= render "picture_index", pictures: @pictures %>
			</div>
		<% else %>
		 	<div class="col-md-12 picture-image">
		 		<%= render "picture_index", pictures: @pictures %>
		 	</div>
		<% end %>
	</div>
</div>