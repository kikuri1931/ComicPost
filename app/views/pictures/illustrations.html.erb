<div class="container">
	<div class="row">
		<h1 class="title">イラスト</h1>
			<div class="picture-image">
				<% @illustration_pictures.each do |picture| %>
					<div class="image">
						<%= link_to picture_path(picture), style: "margin: 20px 0 40px;" do %>
							<%= attachment_image_tag picture.picture_images[0], :image, size: "180x180", format: 'jpeg', fallback: "no_image_square.jpg" %>
							<%= picture.title %>
						<% end %>
					</div>
				<% end %>
			</div>
	</div>
</div>